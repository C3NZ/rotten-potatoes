<div class="card" id="{{this._id}}">
	<div class="card-block">
		<h4 class="card-title">{{this.title}}</h4>
		<p class="card-body">{{this.content}}</p>
		<p>
			<button onclick="deleteComment(&quot;{{this._id}}&quot;)" class="btn btn-primary delete-comment">Delete</button>
		</p>
	</div>

</div>
